---
const { title, description, pubDate, tags, image } = Astro.props;
---

<article class="prose mx-auto px-4 py-8">
    <h1>{title}</h1>
    <p class="text-sm text-gray-500">Published on {new Date(pubDate).toLocaleDateString()}</p>
    <p class="italic">{description}</p>

    <slot />

    <div class="mt-8">
        <strong>Tags:</strong> {tags.join(", ")}
    </div>
</article>

